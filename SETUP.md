# แอปบริหารสหกรณ์ออมทรัพย์ครู — คู่มือติดตั้งและใช้งาน

ระบบบริหารจัดการสหกรณ์ออมทรัพย์ครู (อ้างอิงบริบทสหกรณ์ออมทรัพย์ครูหนองคาย) ครอบคลุมทั้ง 5 โมดูลตามที่ออกแบบไว้ใน [`docs/design/`](docs/design/00-สารบัญ.md):

1. เงินฝาก-หุ้น-เงินกู้ของสมาชิก
2. สวัสดิการและเงินสงเคราะห์
3. รายงานผู้บริหาร/การเงิน
4. แจ้งเตือน/สื่อสารกับสมาชิก
5. นำเข้า/กระทบยอด Statement

เอกสารนี้อธิบายเฉพาะ **วิธีติดตั้งและรันโปรเจกต์** — ดูเหตุผลการออกแบบ/สถาปัตยกรรม/ข้อสมมติฐานที่ [`docs/design/`](docs/design/00-สารบัญ.md)

## เทคสแต็ก

| ส่วน | เทคโนโลยี |
|---|---|
| Framework | Next.js 16 (App Router, TypeScript, Turbopack) |
| UI | React 19 + Tailwind CSS v4 |
| ฐานข้อมูล | Prisma ORM 7 + SQLite (ผ่าน `@prisma/adapter-better-sqlite3`) |
| การยืนยันตัวตน | เซสชันแบบ JWT (`jose`) เก็บใน httpOnly cookie + รหัสผ่านแฮชด้วย `bcryptjs` |

> **หมายเหตุ**: ใช้ SQLite สำหรับ dev/demo เนื่องจากไม่มี PostgreSQL server ให้ใช้งานในสภาพแวดล้อมที่พัฒนาเอกสารชุดนี้ Schema ออกแบบให้ย้ายไป PostgreSQL ได้โดยเปลี่ยน `provider`/driver adapter ใน `prisma/schema.prisma` และ `lib/prisma.ts` เท่านั้น ตามที่ระบุไว้ใน [`docs/design/04-สถาปัตยกรรมระบบและเทคโนโลยี.md`](docs/design/04-สถาปัตยกรรมระบบและเทคโนโลยี.md)

## ข้อกำหนดเบื้องต้น

- Node.js 22 ขึ้นไป
- npm

## ติดตั้งและรันครั้งแรก

```bash
npm install
npx prisma db push      # สร้างไฟล์ฐานข้อมูล SQLite ตาม schema
npm run db:seed         # ใส่ข้อมูลตัวอย่างสำหรับทดลองใช้งาน
npm run dev             # เปิดเซิร์ฟเวอร์ dev ที่ http://localhost:3000
```

ไฟล์ `.env` มีค่าเริ่มต้นสำหรับ dev อยู่แล้ว (ดู `.env.example`) ไม่ต้องแก้ไขอะไรเพิ่มสำหรับการรันในเครื่อง

## บัญชีตัวอย่างสำหรับทดลองใช้งาน

หลังรัน `npm run db:seed` แล้ว เข้าสู่ระบบที่ `/login` ด้วยบัญชีใดบัญชีหนึ่ง (รหัสผ่านเดียวกันหมด: `password123`):

| ชื่อผู้ใช้ | บทบาท | เข้าใช้งานฝั่ง |
|---|---|---|
| `finance01` | เจ้าหน้าที่การเงิน | Back Office |
| `loan01` | เจ้าหน้าที่สินเชื่อ | Back Office |
| `manager01` | ผู้จัดการ | Back Office (รวมรายงานผู้บริหาร) |
| `admin01` | ผู้ดูแลระบบ | Back Office (สิทธิ์สูงสุด) |
| `member01` | สมาชิก (ครูสมชาย ใจดี) | พอร์ทัลสมาชิก |

ข้อมูลตัวอย่างประกอบด้วยสมาชิก 4 คน, เงินกู้ที่อยู่ในสถานะต่างๆ (รอพิจารณา/เบิกจ่ายแล้ว/มีงวดค้างชำระ), กองทุนสวัสดิการ 3 กองทุน และคำขอสวัสดิการตัวอย่าง — พร้อมให้ทดลองทุกโมดูลได้ทันที

## คำสั่งที่ใช้บ่อย

| คำสั่ง | ใช้ทำอะไร |
|---|---|
| `npm run dev` | รันเซิร์ฟเวอร์ dev |
| `npm run build` | สร้าง production build |
| `npm run start` | รัน production build (ต้อง `build` ก่อน) |
| `npm run typecheck` | ตรวจสอบ TypeScript ทั้งโปรเจกต์ |
| `npm run db:generate` | สร้าง Prisma Client ใหม่หลังแก้ `schema.prisma` |
| `npm run db:push` | ซิงก์ฐานข้อมูลกับ `schema.prisma` (dev เท่านั้น ไม่มีระบบ migration) |
| `npm run db:seed` | ล้างข้อมูลและใส่ข้อมูลตัวอย่างใหม่ทั้งหมด |

หลังแก้ไข `prisma/schema.prisma` ต้องรัน `db:generate` และ `db:push` ใหม่เสมอ

## โครงสร้างโปรเจกต์

```
app/
  back-office/     หน้าเจ้าหน้าที่สหกรณ์ (แต่ละโฟลเดอร์ = 1 โมดูล)
  portal/          หน้าสมาชิก (self-service)
  login/           หน้าเข้าสู่ระบบ
lib/
  actions/         Server Actions ของแต่ละโมดูล (การตรวจสิทธิ์ทำที่นี่)
  session.ts       จัดการเซสชัน/JWT/สิทธิ์การเข้าถึง
  prisma.ts        Prisma Client singleton
  money.ts         แปลงหน่วยเงิน (บาท ↔ สตางค์)
  loan-schedule.ts คำนวณตารางผ่อนชำระ
  csv.ts           อ่านไฟล์ CSV สำหรับโมดูลนำเข้า/กระทบยอด
prisma/
  schema.prisma    โครงสร้างฐานข้อมูลทั้งหมด
  seed.ts          ข้อมูลตัวอย่าง
proxy.ts           ตรวจสอบการเข้าสู่ระบบเบื้องต้นก่อนเข้าถึงหน้าที่ต้อง login
  (เทียบเท่า middleware.ts ในชื่อเดิมของ Next.js เวอร์ชันก่อนหน้า)
docs/design/        เอกสารวิเคราะห์และออกแบบระบบ (ก่อนเริ่มเขียนโค้ด)
```

**หลักการสำคัญของโค้ด**: การตรวจสอบสิทธิ์ (`requireRole(...)`) ทำอยู่ *ภายใน* ทุก Server Action เสมอ ไม่ได้พึ่งแค่การซ่อนปุ่มในหน้าจอหรือ layout guard เพียงอย่างเดียว — ดูเหตุผลที่ [`docs/design/05-ขอบเขตที่ไม่ใช่ฟังก์ชันและความปลอดภัย.md`](docs/design/05-ขอบเขตที่ไม่ใช่ฟังก์ชันและความปลอดภัย.md)

## ข้อจำกัดที่ทราบอยู่แล้ว (MVP)

- **ฐานข้อมูล**: SQLite สำหรับ dev เท่านั้น ยังไม่ได้ตั้งค่าเชื่อมต่อ PostgreSQL จริง
- **LINE OA/SMS/อีเมล**: ยังไม่ได้เชื่อมต่อผู้ให้บริการจริง (ไม่มี credential) — การแจ้งเตือนช่องทางเหล่านี้จะถูกบันทึกสถานะ "รอเชื่อมต่อ" เท่านั้น มีเพียงการแจ้งเตือนในแอป (พอร์ทัลสมาชิก) ที่ทำงานได้จริง
- **รูปแบบไฟล์นำเข้า** (โมดูล 5): เป็นรูปแบบ CSV ที่กำหนดขึ้นเองชั่วคราว เนื่องจากยังไม่ทราบรูปแบบไฟล์จริงจากต้นสังกัด/ธนาคาร — ดู [`docs/design/07-ข้อสมมติฐานที่ต้องยืนยัน.md`](docs/design/07-ข้อสมมติฐานที่ต้องยืนยัน.md)
- **งบดุล/งบกำไรขาดทุนแบบเต็มรูปแบบ**: ยังไม่รองรับ (ต้องมี chart of accounts เพิ่มเติม) มีเฉพาะแดชบอร์ดสรุปและรายงานหนี้ค้างชำระ
- ไม่มีตัวตั้งเวลาอัตโนมัติ (cron) ในระบบ — งานที่ควรรันตามรอบ (ดอกเบี้ยเงินฝาก, เรียกเก็บรายเดือน, แจ้งเตือนงวดใกล้ครบกำหนด) เป็นปุ่มให้เจ้าหน้าที่กดเองทั้งหมด
